import { Outlet } from 'react-router-dom';

const Navigation = () => {
    return (
        <div>
            THIS IS NAVIGATION
            <Outlet />
        </div>
    );
};

export default Navigation;
